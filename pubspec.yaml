name: companion
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0-alpha.1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.1.0
  http: ^1.6.0
  web_socket_channel: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  fonts:
    - family: StardewValley
      fonts:
        - asset: assets/fonts/StardewValley.ttf
